# Core dependencies
boto3>=1.26.0
numpy>=1.21.0
pandas>=1.3.0
SimpleITK>=2.2.0
pydicom>=2.3.0
totalsegmentator>=1.5.0
rt-utils>=1.2.0
nibabel>=3.2.0
# NIfTI blosc/zstd (e.g. HECKTOR). ABI errors usually mean mixed installs (system numpy + ~/.local blosc2).
# Prefer a venv + `pip install -r requirements.txt`, or PYTHONNOUSERSITE=1, then:
#   python -m pip uninstall -y blosc2 numpy && python -m pip install --no-cache-dir numpy blosc2
blosc2>=2.5.0
matplotlib>=3.5.0
scikit-image>=0.19.0
scipy>=1.7.0
opencv-python>=4.5.0
python-dotenv>=0.19.0

# Progress bars and parallel processing
p-tqdm>=1.4.0
tqdm>=4.64.0

# Visualization (for research notebooks)
seaborn>=0.12.0

# Surface DICE calculation (evaluation metrics)
surface-distance @ git+https://github.com/google-deepmind/surface-distance.git

